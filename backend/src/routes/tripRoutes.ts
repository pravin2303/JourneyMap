import express from "express";
const router = express.Router();
// Add endpoints for creating, updating, deleting trips.
router.post("/", /* controller */);
router.get("/", /* controller */);
export default router;